% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_functions.R
\name{remove_empty}
\alias{remove_empty}
\title{Remove Empty Rows or Columns from a Data Frame}
\usage{
remove_empty(.data, which = "rows")
}
\arguments{
\item{.data}{A data frame.}

\item{which}{A character string, either "rows" (the default) or "cols",
specifying whether to remove empty rows or empty columns.}
}
\value{
A data frame with the empty rows or columns removed.
}
\description{
This function inspects a data frame and removes all rows or all columns
that are composed entirely of \code{NA} values.
}
\examples{
# Create a sample data frame for the example
messy_df <- data.frame(
  x = c(1, NA, 3),
  y = c("a", NA, "c"),
  z = c(NA, NA, NA)
)
messy_df[2, ] <- NA # Create a fully empty row

# Remove empty rows (the default)
remove_empty(messy_df)

# Remove empty columns
remove_empty(messy_df, which = "cols")
}
